# Workflowy MCP – copy to .env.local and fill values
# Use on Vercel: add these in Project → Settings → Environment Variables

# ---- Required ----

# Neon PostgreSQL connection string
# From: https://neon.tech → Create Project → Connection string
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# 64-char hex (32 bytes). Generate: openssl rand -hex 32
ENCRYPTION_KEY=

# 64-char hex (32 bytes). Generate: openssl rand -hex 32
JWT_SECRET=

# Your deployment URL (no trailing slash). Vercel: https://your-project.vercel.app
OAUTH_ISSUER=https://your-project.vercel.app

# ---- Optional ----

# 64-char hex. Generate: openssl rand -hex 32
# If set: clients must send x-oauth-registration-secret to POST /api/oauth/register
# OAUTH_REGISTRATION_SECRET=
